# wisp-dashboard

A standalone React + Vite browser dashboard and console for the **Wisp** broker
(the daemon that leases short-lived, root-access throwaway containers). It runs
alongside a wisp instance and talks to it over wisp's HTTP + WebSocket API.

This is a seed commit; the app is built out via grunt tasks against this repo.
